type templateHead = {
    meta?: {
        type?: string,
        description?: string,
        image?: string,
        path?: string
    },
    assets?: string,
    styles?: string[],
    scripts?: string[]
}
